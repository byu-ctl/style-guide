/* Sidebar */
body.sidebar { background: white url("../images/sidebar-light.png") top left repeat-y; background-attachment: fixed; }
#sidenav { background: transparent url("../images/sidebar-top.png") top left no-repeat; margin: 0; border: none; }
#sidenav h2 { padding-left: 38px; line-height: 30px; font-size: 21px; color: #444; margin: 20px 10px 10px 14px; background: transparent url("../images/icon-30.png") left top no-repeat; }
#sidenav a { font-size: 15px; border: none; border-top: 1px solid transparent; border-bottom: 1px solid transparent; background-color: transparent; color: #444; padding: 5px 8px 5px 30px; @include nate_transition(background 0.1s ease); }
#sidenav a:hover { background: $yellow2; margin-right: 0; border-color: #eeeebe; }
#sidenav a.edit:hover { border-color: #fff; }
#sidenav a:active { padding: 5px 7px 5px 31px; }
#sidenav a.current:active { padding: 5px 8px 5px 30px; }
#sidenav a.current, #sidenav a.current:hover { background: #fff; color: #444; border-color: #ddd; margin-right: 0; }
#sidenav .sub a, #sidenav .sub a:link { padding-left: 45px; }
#sidenav .sub .sub a, #sidenav .sub .sub a:link { padding-left: 60px; }
#sidenav li { overflow: hidden; }
.pagenav a { display: block; }



/* Button Bar */
.sidebar #buttonbar { margin-left: 225px; }
#buttonbar.clearfix:after { clear: right; }
#buttonbar { background: $blue9; border-bottom: 1px solid $blue7; padding: 0 2em; }
#buttonbar a { display: block; float: right; padding: .2em .75em; margin: 0; font-weight: bold; text-decoration: none; border-left: 1px solid transparent; border-right: 1px solid transparent; position: relative; }
#buttonbar a:hover { background: #fff; border-color: $blue7; }